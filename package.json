{
  "name": "cs394-advancedweb-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd my-react-app && npm install && npm run build",
    "start": "cd my-react-app && npm run preview -- --host 0.0.0.0 --port ${PORT:-8080}"
  }
}
