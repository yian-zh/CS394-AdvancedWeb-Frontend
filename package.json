{
  "name": "cs394-advancedweb-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd my-react-app && npm install && npm run build",
    "start": "cd my-react-app && npx serve -s dist -l ${PORT:-8080}"
  }
}
